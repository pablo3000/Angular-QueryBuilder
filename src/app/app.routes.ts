import { Routes } from '@angular/router';
import {QueryBuilderDemoComponent} from './demo/query-builder-demo/query-builder-demo.component';

export const routes: Routes = [
  {path:'',
    component: QueryBuilderDemoComponent
  }

];
